export default function Page() {
  return (
    <main style={{ padding: 40 }}>
      <h1>🚀 ProjectSensei — LIVE PREVIEW Test!</h1>
      <p>This is a minimal Next.js app (exported statically).</p>
    </main>
  );
}
